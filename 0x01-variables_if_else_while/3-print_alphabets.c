#include <stdio.h>
/**
 * main - Entry point
 * Return: Alwayes 0 (Success)
*/
int main(void)
{
	char c;

	char u;

	c = 'a';
	u = 'A';
	while
		(c <= 'z') {
			putchar(c);
			c++;
		}
	while
		(u <= 'Z') {	
			putchar(u);
			u++;
		}
	putchar('\n');
	return (0);
}

